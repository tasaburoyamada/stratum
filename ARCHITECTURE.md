# Stratum: 内部アーキテクチャ

本ドキュメントは、HV-CAD 準拠の知識エンジン Stratum の物理構造と論理的不変条件（Invariants）を規定します。

## 1. 物理層 (Physical Layer)

### 決定論的 ID 生成 (Deterministic ID)
全てのデータユニット（`Node`）は、以下の要素から生成される **Blake3 ハッシュ** によって一意に識別されます。
- `NodeContent`: テキストまたはバイナリデータ
- `TypedMetadata`: JSON メタデータ（`BTreeMap` により正規化された順序でシリアライズ）

これにより、異なるセッションや環境であっても、同一の知識に対しては常に同一の ID が割り振られることが物理的に保証されます。

### ACID 永続化ストレージ
`redb` をバックエンドに採用し、以下の順序で書き込みを行うことで、プロセス異常終了時でも検索インデックスと実データの乖離を防止します。
1. `redb` トランザクション内でのデータ書き込み
2. トランザクションのコミット（物理ディスクへの同期）
3. メモリ上の検索インデックス（HNSW）の更新

## 2. 知能層 (Logic Layer: Stratification)

Stratum は、情報を「フラットな記憶」ではなく「重なりのある地層」として扱います。

### Stratification Metrics
- **Confidence (信頼度)**: 情報の事実性や検証可能性（0.0 - 1.0）。
- **Importance (重要度)**: 戦略的・技術的な価値（0.0 - 1.0）。
- **Timestamp (時間軸)**: 情報の発生または更新時刻。

### Temporal Decay (時間的減衰)
情報の価値は時間と共に減衰するという物理法則をシミュレートします。
$$Score_{boosted} = Score_{base} \times 0.98^{age\_in\_days}$$
この減衰曲線により、数ヶ月前の古い推測よりも、数時間前の新しい事実が自動的に優先されます。

## 3. 統治層 (Governance Layer: Vlog Alignment)

`VlogBiasPostprocessor` は、`.vlog` ファイルに記述された人間の「現在の関心」を検索スコアに物理的に注入します。

| Vlog 要素 | 反映対象 | 効果 |
| :--- | :--- | :--- |
| `@BIAS` | L1: ジャンル・属性 | 特定分野の定数的なブースト |
| `[[CONCEPT]]` | L2: セマンティック概念 | 埋め込みベクトルの類似度による意味的ブースト |
| `@CTX` | L3: 高次意図・目標 | 構造化された「現在のゴール」への一致度による最大ブースト |

## 4. メモリ最適化
`NodeContent::Purged` 状態をサポート。Embedding 生成後の重いテキストデータをメモリから解放しつつ、メタデータ（`tags`, `intent`）を用いたフォールバック検索により、大規模データ環境下での省メモリと機能性を両立します。
