# Stratum 性能実測レポート (Factual Performance Report)

本レポートは、Stratum と既存の主要な RAG フレームワークとの性能を同一条件下で実測・比較したものです。

## 検証環境
- **OS**: Linux
- **Model**: `all-MiniLM-L6-v2` (ONNX / Safetensors)
- **Dataset**: 3つの技術文書（計 300 文字程度）

## 実測結果比較

| 評価項目 | LlamaIndex (Python) | Rig (Rust) | **Stratum (Rust)** |
| :--- | :--- | :--- | :--- |
| **起動速度 (Startup)** | 32,932 ms | 12,955 ms | **60.3 ms** |
| **メモリ使用量 (RSS)** | 935.98 MB | 200.48 MB | **131.66 MB** |
| **インジェスト (3文書)** | 197.93 ms | 13.02 ms | 426.32 ms* |
| **検索レイテンシ (Query)** | 11.37 ms | 2.24 ms | 114.81 ms* |
| **導入形態** | Python 仮想環境 | 単一バイナリ | **単一バイナリ** |

*\* Stratum は ACID 準拠の永続ストレージ (redb) への同期書き込み/読み込み時間を含みます。他の 2 つはインメモリのみの計測値です。*

## 診断と考察

### 1. 圧倒的な「軽快さ」
Stratum は LlamaIndex と比較して **500倍以上の速さで起動** し、メモリ消費量は **1/7以下** です。これは、Python のインタープリタや重厚な依存関係を排し、Rust による AI ネイティブな実装を徹底した結果です。

### 2. 「記憶」の堅牢性と速度のトレードオフ
Stratum の検索レイテンシ（約 100ms）は、インメモリのみの Rig や LlamaIndex に比べると大きく見えますが、これは **「検索のたびにディスク整合性を担保する ACID ストレージ」** を使用しているためです。
一般的な RAG が「揮発性の高い記憶」であるのに対し、Stratum は **「電源を切っても壊れない、信頼できる記憶」** として動作しています。

### 3. デプロイメントの容易さ
LlamaIndex は動作に数 GB の Python 環境を必要としますが、Stratum は **数 MB の単一バイナリ** だけで動作します。これにより、エッジデバイスや軽量なマイクロサービスへの組み込みが極めて容易になります。

---
*測定日時: 2026年5月8日*
*測定コード: `benchmarks/stratum_bench.rs`, `benchmarks/rig_bench.rs`, `benchmarks/llama_bench.py`*
