[package]
name = "rig_runner"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
rig-core = "0.3.0"
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
anyhow = "1.0"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

